# Next.js server-side proxy. Do not expose database credentials to the browser.
BACKEND_URL=http://127.0.0.1:8000
# Optional public MapLibre style URL. Leave blank to use the bundled OpenStreetMap raster style.
NEXT_PUBLIC_MAP_STYLE_URL=
# Existing synthetic chat replay remains separate from the Part 1 backend.
# Omit DEMO_MODE here to retain the existing replay behavior.
